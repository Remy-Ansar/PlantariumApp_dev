{% extends "base.html.twig" %}

{% block title %}
    Accueil
    {{ parent() }}
{% endblock %}
{% block body %}
<h1>Plantarium</h1>

<main>
<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>Nom</th>
            <th>Description</th>
            <th>Image</th>
            <th>Visible</th>
            <th>Photo</th>
            <th>Couleurs</th>
            <th>Saisons</th>
            <th>Famille</th>
            <th>Espèce</th>
            <th>Couleurs</th>
            <th>Catégorie</th>
            <th>Action</th>
        </tr>
    </thead>
    {# <tbody>
 
        
    {% for plant in plants %}
        <tr>
            <td>{{ user.id }}</td>
            <td>{{ user.email }}</td>
            <td>{{ user.roles|join(', ') }}</td>
            <td>{{ user.UserInfos.getFullName() }}</td>
            <td>{{ user.UserInfos.getLevel() }}</td>
            <td><img src="{{ vich_uploader_asset( user.UserInfos, 'image') }}" alt="{{ user.UserInfos }}" loading="lazy" class="rounded" width="50px" height="50px"></td>
           
            <td><a href="{{ path('admin.users.edit', {id: user.id}) }}">Modifier</a></td>
            <td>
            <form 
                    action="{{path('admin.users.delete', {id: user.id}) }}" 
                    method="POST" 
                    onsubmit="return confirm('Êtes-vous certain de vouloir supprimer cet utilisateur? Cette action est irréversible')"
                    enctype="multipart/form-data">
                    <input type="hidden" name="token" value="{{ csrf_token('delete'~ user.id) }}">
                    <button type="submit">Supprimer</button>
            </form>
            </td>
        </tr>
        {% endfor %}
    </tbody> #}
</table>
</main>

{% endblock %}