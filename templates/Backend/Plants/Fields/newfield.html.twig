{% extends 'base.html.twig' %}

{% block title %}
	Ajouter des champs de selection
	{{ parent() }}
{% endblock %}

{% block body %}

<main>
		<h1>Ajouter des champs de selection</h1>
	<section>
		{% include 'Backend/Plants/_form.html.twig' with {button_label: 'Créer'} %}
		<a href="{{ path('editor.plants.index') }}" class="button">Retour à la liste des plantes</a>
                {% for message in app.flashes('success') %}
        <div class="alert alert-success">
            {{ message }}
        </div>
    {% endfor %}
	</section>
</main>
{% endblock %}
